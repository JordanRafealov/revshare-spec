# RevShare Spec + Research

Password-protected single-page handoff doc for the RevShare engineering build.

## Contents (all in one page)

- **Product Spec** -- full product specification, tech stack, database schema, Stripe Connect flow, GTM, revenue projections
- **Research** -- 28 case studies (what failed, what won), AI contract platforms, regulatory landscape
- **Build Resources for Thomas** -- Stripe Connect deep dive, Claude API tool use, Clerk + Supabase native integration, webhooks, ESIGN/UETA compliance, PDF generation, production gotchas

## Access

`index.html` is encrypted with StatiCrypt (AES-256-GCM). Password required.

## Source of Truth

The live, continuously-updated version lives at `docs.dopaminedigital.io/internal/revshare` (behind Cloudflare Access). This repo is a static snapshot for engineering handoff.
